import 'package:flutter/material.dart';

class AppRadius {
  static const small = Radius.circular(4);
  static const medium = Radius.circular(8);
  static const large = Radius.circular(12);
  static const xLarge = Radius.circular(16);
  static const xxLarge = Radius.circular(20);
  static const xxxLarge = Radius.circular(24);
  static const circle = Radius.circular(28);
  
  static const BorderRadius smallAll = BorderRadius.all(small);
  static const BorderRadius mediumAll = BorderRadius.all(medium);
  static const BorderRadius largeAll = BorderRadius.all(large);
  static const BorderRadius xLargeAll = BorderRadius.all(xLarge);
  static const BorderRadius xxLargeAll = BorderRadius.all(xxLarge);
  static const BorderRadius xxxLargeAll = BorderRadius.all(xxxLarge);
  static const BorderRadius circleAll = BorderRadius.all(circle);
}

